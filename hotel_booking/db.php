<?php
/*$host = "localhost";
$user = "root";
$password = "";
$dbname = "hotel_db";

$conn = new mysqli($host, $user, $password, $dbname);

if ($conn->connect_error) {
  die("Connection failed: " . $conn->connect_error);
}


C/*REATE DATABASE hotel_db;

USE hotel_db;

CREATE TABLE bookings (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100),
  checkin DATE,
  checkout DATE,
  room VARCHAR(50)
);*/

?>

